task: Build an authenticated backend endpoint for account summaries
context:
  project: Example App
  constraints:
    - Use the existing service layer
    - Preserve current response shape
  files:
    - src/api/accounts.ts
    - src/services/accountService.ts
prior_artifacts: []
current_phase: Phase 2 - Backend implementation
originating_role: master
acceptance_criteria:
  - Endpoint requires authentication
  - Endpoint returns account summary data
  - Work is ready for validator review
