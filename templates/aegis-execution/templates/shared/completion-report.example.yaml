status: completed
summary: Added the requested provider-neutral workflow example and verified it remains within ticket scope.
artifacts:
  - templates/example-workflow/workflow.example.yaml
findings:
  - No boundary conflicts found.
next_recommended_role: validator
changed_files:
  - templates/example-workflow/workflow.example.yaml
verification:
  commands:
    - command: git diff -- templates/example-workflow/
      result: passed
      notes: Reviewed the scoped diff and confirmed only allowed files changed.
  manual:
    required: true
    status: completed
    steps:
      - Confirmed the example is provider-neutral.
      - Confirmed all changed files are inside the allowed areas.
